# SPRINT

Immediate technical tasks to execute.

1. [x] Build standard CRUD routes for Prospects
3. [ ] Update the `EmailActivity` model to track retry attempts and status
4. [ ] Modify the `EmailService` to handle retry logic based on the retry count and status
5. [ ] Add a scheduled job to periodically check and retry emails that have failed due to soft bounces
